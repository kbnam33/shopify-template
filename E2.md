# TECHNICAL PLAN — Suppress Hover Background on Transparent Header

## TASK 1 — `sections/header.liquid` — Disable the hover underlay on transparent state

Inside the `{% stylesheet %}` block of `sections/header.liquid`, locate the rule block that targets `.header[transparent]` — specifically the nested rules that handle hover and focus-within states. These are the selectors that currently restore the solid background when a nav link is hovered.

Find the nested rule that matches `.header[transparent]:has(.menu-list__link:not([aria-haspopup]):hover)` or similar hover selectors within the `[transparent]` block. These rules currently un-inherit the color variables, which causes the solid maroon underlay to fade in.

Add a further condition to each of these hover selectors so they only fire when `data-sticky-state` is active — meaning the header has already scrolled into sticky mode. Specifically, the hover-triggered color restoration should only apply when the header is NOT in the `transparent='not-sticky'` state at the top of the page.

The condition to add to each hover selector inside `[transparent]` is: the selector should also require the absence of `transparent='not-sticky'` attribute — i.e. only trigger when the header is in sticky/scrolled state, not when it is floating over the hero.

---

## Push after changes:

Push only `sections/header.liquid` to theme `188273557786` on store `x9iqze-nb.myshopify.com`.